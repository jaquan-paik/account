upstream django_admin {
    server unix:/var/local/nginx/sockets/admin.sock;
}

server {
    listen 80;
    server_name account-admin.dev.ridi.io;

    location / {
        include         /etc/nginx/uwsgi_params;
        uwsgi_pass      django_admin;
    }
}
