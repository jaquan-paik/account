image: ridibooks/python:gitlab-ci-2018.11.12.1

stages:
- test

test:
  stage: test
  script:
  - export TAG=${CI_COMMIT_SHA::8}
  - echo $TAG
  - make pm-test-up
  - echo $TAG && docker exec account-www-123456 ls
  - docker exec account-www-123456 ls docs
  - docker exec account-www-123456 ls src
  - docker ps
#  - make pm-test-run
  after_script:
  - make pm-test-down